services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./backend/data:/app/backend/data  # persist SQLite database
    command: node /app/backend/dist/server.js